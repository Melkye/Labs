#pragma once

#include "CustomString.h" // include this explicitly?
#include "Text.h"			// or this is sufficient?

char* EnterCharArray();
CustomString* EnterStringArray(int& nRows);
void PrintText(Text text);